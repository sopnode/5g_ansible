---
duration: "{{ ping_duration | default(30) }}"
sleep: "{{ ping_sleep | default(5) }}"
async_stagger: 0

ping_scenarios:
  - {packet_size: 64, extra_args: ""}
  - {packet_size: 1000, extra_args: "-s 992"} # 8 bytes of header data